<ui:ContentDialog x:Class="TamoPOS.Controls.NewPurchaseOrderDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TamoPOS.Controls"
             mc:Ignorable="d"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" 
             ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
             ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
             PrimaryButtonText = "Guardar"
             CloseButtonText="Cancel"
             DialogWidth="400"
             d:DesignHeight="450" d:DesignWidth="800">
    <ui:ContentDialog.Resources>
        <Style BasedOn="{StaticResource {x:Type ui:ContentDialog}}" TargetType="{x:Type local:NewPurchaseOrderDialog}" />
    </ui:ContentDialog.Resources>
    <Grid Width="350">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Proveedor" HorizontalAlignment="Left" />
        <ui:AutoSuggestBox 
            Grid.Row="0" 
            Grid.Column="1" 
            x:Name="SupplierBox"
            TextChanged="SupplierBox_TextChanged"
            HorizontalAlignment="Stretch" />

        <TextBlock 
            Grid.Row="1" 
            Grid.Column="0" 
            Text="Fecha" 
            HorizontalAlignment="Left" />
        <DatePicker 
            x:Name="DatePickerField"
            SelectedDateChanged="SelectedDate_SelectedDateChanged"
            Grid.Row="1" 
            Grid.Column="1" />
    </Grid>
</ui:ContentDialog>
