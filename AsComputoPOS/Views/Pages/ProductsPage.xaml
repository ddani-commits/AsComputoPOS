<Page x:Class="TamoPOS.Views.Pages.ProductsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TamoPOS.Views.Pages"
      mc:Ignorable="d" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      d:DesignHeight="450" d:DesignWidth="800"
      xmlns:controls="clr-namespace:TamoPOS.Controls"
      Title="ProductsPage">

    <Grid Width="auto" HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
 
                <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

            <Button 
                Grid.Row="0"
                x:Name="OpenDialogButton"
                Content="Nuevo producto" 
                Command="{Binding ViewModel.ShowDialogCommand}"/>
                
        <ui:TextBox PlaceholderText="Buscar" Grid.Row="0" MinWidth="300" HorizontalAlignment="Right" Grid.Column="1" Margin="0, 0, 10, 0" />

        <ui:DataGrid Grid.Row="1" Grid.ColumnSpan="2" ItemsSource="{Binding ViewModel.ProductsList}" AutoGenerateColumns="False">
            <ui:DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ProductId}"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Precio" Binding="{Binding ProductPurchase[0].SalePrice}"/>
                <DataGridTextColumn Header="Categoría"/>
                <DataGridTextColumn Header="Existencias" Binding="{Binding TotalStockResumen}"/>
                <DataGridTemplateColumn Header="Detalles">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button HorizontalAlignment="Center" Command="{Binding DataContext.ViewModel.NavigateToProductDetailsCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                    CommandParameter="{Binding ProductId}">
                                <ui:SymbolIcon Symbol="ArrowEnter20"/>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </ui:DataGrid.Columns>
        </ui:DataGrid>
    </Grid>
</Page>