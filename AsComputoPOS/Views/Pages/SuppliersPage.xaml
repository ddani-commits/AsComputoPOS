<Page x:Class="TamoPOS.Views.Pages.SuppliersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TamoPOS.Views.Pages"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SuppliersPage">

    <StackPanel>
        <Grid Margin="5" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" >
                <Button Command="{Binding ShowSignInContentDialogCommand}" HorizontalAlignment="Left"  Margin="0,0,5,0">
                    <ui:SymbolIcon Symbol="Add28"/>
                </Button>
                <Button Command="{Binding SaveSupplierCommand}" HorizontalAlignment="Left" Margin="0,0,5,0">
                    <ui:SymbolIcon Symbol="Save28"  />
                </Button>
            </StackPanel>
            <Button Command="{Binding SelectFolderCommand}" HorizontalAlignment="Right"  VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                <ui:SymbolIcon Symbol="ArrowDownload16" Height="20"/>
                    <TextBlock Text="Exportar" Margin="0,0,5,0"/>
                </StackPanel>
            </Button>
        </Grid>
        <Grid>
            <ui:DataGrid ItemsSource="{Binding SuppliersList}" AutoGenerateColumns="False" Margin="5">
                <ui:DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding SupplierId}" Width="Auto"/>
                    <DataGridTextColumn Header="Nombre de la compañía" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Nombre del contacto" Binding="{Binding ContactName}" Width="*"/>
                    <DataGridTextColumn Header="Dirección" Binding="{Binding Address}" Width="*"/>
                    <DataGridTextColumn Header="Correo" Binding="{Binding Email}" Width="*"/>
                    <DataGridTextColumn Header="Teléfono" Binding="{Binding Phone}" Width="*"/>
                    <DataGridCheckBoxColumn Header="Activo" Binding="{Binding IsActive, UpdateSourceTrigger=PropertyChanged}"/>
                    <DataGridTemplateColumn Header="Acción">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button
                                        Command="{Binding DataContext.DeleteSupplierCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}" HorizontalAlignment="Center">
                                    <ui:SymbolIcon Symbol="Delete20" Foreground="Red"/>
                                </Button>

                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </ui:DataGrid.Columns>
            </ui:DataGrid>
        </Grid>
        
    </StackPanel>
</Page>
